# Module: node

Installs the Node.js runtime via nvm (Node Version Manager) and activates the LTS release.

## Packages

| Package | Manager | Purpose |
|---|---|---|
| `nvm` | AUR | Node Version Manager |

## Verification

Checks `node` and `npm` exist in `$PATH`.

## Dependencies

- `core`
