# Generated by matugen — do not edit manually.
# Source: ~/.config/matugen/templates/hyprland-colors.conf
# Re-generate: matugen image ~/Pictures/Wallpapers/current.jpg

$primary   = rgb({{colors.primary.default.hex | replace(from="#", to="")}})
$secondary = rgb({{colors.secondary.default.hex | replace(from="#", to="")}})
$tertiary  = rgb({{colors.tertiary.default.hex | replace(from="#", to="")}})
$bg        = rgb({{colors.background.default.hex | replace(from="#", to="")}})
$surface   = rgb({{colors.surface_container.default.hex | replace(from="#", to="")}})
$surface_low = rgb({{colors.surface_container_low.default.hex | replace(from="#", to="")}})
$text      = rgb({{colors.on_background.default.hex | replace(from="#", to="")}})
$subtext   = rgb({{colors.on_surface_variant.default.hex | replace(from="#", to="")}})
$border    = rgb({{colors.outline_variant.default.hex | replace(from="#", to="")}})
$error     = rgb({{colors.error.default.hex | replace(from="#", to="")}})
